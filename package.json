{
  "name": "n8n-compose-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "changeset": "bunx @changesets/cli",
    "test": "bunx vitest",
    "lint": "bunx eslint . --max-warnings 0",
    "typecheck": "bunx tsc -b --pretty",
    "build": "bun run -w core build && bun run -w cli build"
  }
}
